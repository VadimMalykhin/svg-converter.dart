name: Publish to ghcr.io

on:
  workflow_dispatch:
    inputs:
      runs_on:
        required: true
        type: choice
        description: Runs on
        options:
          - ubuntu-latest
          - self-hosted

env:
  RUNS_ON: ubuntu-latest
  REGISTRY: ghcr.io

jobs:
  build:
    runs-on: ${{ inputs.runs_on }}
    container: dart:beta
    steps:
      - name: Dart Version
        run: dart --version

      - name: Checkout
        uses: actions/checkout@v3

      - name: Compile to executable
        run: dart compile exe bin/svg_converter.dart -o ./build/svg_converter

      - uses: actions/upload-artifact@v3
        with:
          name: build-artifact
          path: |
            build/*

  test:
    needs: build
    runs-on: ${{ inputs.runs_on }}
    steps:
      - name: Test
        run: echo 1

      - uses: actions/download-artifact@v3
        with:
          name: build-artifact

      - name: Display structure of downloaded files
        run: ls -R

      - name: Run artifact
        run: ./svg_converter
