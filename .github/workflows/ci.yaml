name: Run CI

on:
  workflow_dispatch:

jobs:
  test:
    name: Test
    runs-on: self-hosted
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v3

      - name: Download Dart SDK
        uses: dart-lang/setup-dart@v1
        with:
          sdk: beta

      - name: Dart Version
        run: dart --version

      - name: Run tests
        run: dart run test
